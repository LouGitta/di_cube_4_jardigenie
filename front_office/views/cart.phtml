<h2>Votre panier</h2>
<?php if(empty($cart)) : ?>
    <p>Votre panier est vide.</p>
<?php else : ?>
    <table>
        <tr>
            <th>Produit</th>
            <th>Quantité</th>
            <th>Prix unitaire</th>
            <th>Total</th>
            <th>Action</th>
        </tr>
        <?php foreach($products as $product) : ?>
        <tr>
            <td><?= $product['nom'] ?></td>
            <td><?= $product['quantite'] ?></td>
            <td><?= $product['prix'] ?> €</td>
            <td><?= $product['total'] ?> €</td>
            <td>
                <a href="index.php?page=cart&action=delete&id=<?= $product['id'] ?>">Supprimer</a>
            </td>
        </tr>
        <?php endforeach; ?>
    </table>
    <p>Total commande : <?= $total ?> €</p>
    <a href="index.php?page=validation">Valider la commande</a>
<?php endif; ?>
