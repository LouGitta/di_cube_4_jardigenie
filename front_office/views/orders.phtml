<section>
    <div class="container row text-center mt-2">
        <?php if (count($listOrders) === 0): ?>
            <div class="alert alert-info" role="alert">
                Vous n'avez pas encore passé de commande.
            </div>
        <?php else: ?>
            <h2 class="text-center">Mes commandes</h2>
            <p class="text-center">Voici la liste de vos commandes passées sur notre site.</p>
            <?php foreach ($listOrders as $index => $value): ?>
                <div class="col-12 col-sm-6 col-md-4 col-lg-3 mt-3">
                    <div class="card h-100">
                        <div class="card-body d-flex flex-column justify-content-between">
                            <div>
                                <h5 class="card-title">
                                    Commande n° <?= htmlspecialchars($index +1) ?>
                                </h5>
                                <h6 class="card-subtitle mb-2 text-body-secondary">
                                    Date : <?= htmlspecialchars($value['order_date'] ?? '') ?>
                                </h6>
                                <p class="card-text">
                                    Produits commandés : <?= htmlspecialchars($value['number_product'] ?? '0') ?>
                                </p>
                            </div>
                            <div class="mt-auto">
                                <p class="card-text fw-bold">
                                    Total : <?= htmlspecialchars(number_format($value['total_price'] ?? 0, 2, ',', ' ')) ?> €
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        <?php endif; ?>        
    </div>
</section>
