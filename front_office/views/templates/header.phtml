<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-success">
        <div class="container-fluid">
            <a class="navbar-brand" href="?route=home">
                <img src="/di_cube_4_jardigenie/public/images/logojardigenie.png" alt="JardiGenie Logo" height="40">
                Jardigénie
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="?route=home">Accueil</a>
                    </li>
                    <?php if (isset($_SESSION['user'])): ?>
                        <li class="nav-item">
                            <a class="nav-link" href="?route=orders">Commandes</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <?= htmlspecialchars($_SESSION['user']['first_name']) ?>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end bg-white">
                                <li><a class="dropdown-item" href="?route=profile">Mon profil</a></li>
                                <?php if ($_SESSION['user']['is_admin'] == 1): ?>
                                    <li><a class="dropdown-item" href="/di_cube_4_jardigenie/back_office/">Administration</a></li>
                                <?php endif; ?>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="?route=logout">Déconnexion</a></li>
                            </ul>
                        </li>
                    <?php else: ?>
                        <li class="nav-item">
                            <a class="nav-link" href="?route=login">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="?route=register">Inscription</a>
                        </li>
                    <?php endif; ?> 
                    <li class="nav-item">
                        <a href="?route=cart" class="btn btn-outline-light position-relative ms-2">
                            <i class="bi bi-cart3"></i>
                            <?php if($totalItems > 0): ?>
                                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                    <?= $totalItems ?>
                                    <span class="visually-hidden">articles dans le panier</span>
                                </span>
                            <?php endif; ?>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>