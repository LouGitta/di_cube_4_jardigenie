<div class="container mt-4">
  <div class="card shadow-sm">
    <div class="card-header bg-primary text-white">
      <h5 class="mb-0">Ajout d’un nouvel utilisateur</h5>
    </div>
    <div class="card-body">
      <?php include_once('views/errors.phtml'); ?>
      <form enctype="multipart/form-data" method="POST" action="<?= htmlspecialchars($route) ?>">
        <div class="mb-3">
          <label for="first_name" class="form-label">Prénom</label>
          <input type="text" class="form-control" name="first_name" id="first_name" value="<?= htmlspecialchars($newUser['first_name']) ?>">
        </div>

        <div class="mb-3">
          <label for="last_name" class="form-label">Nom</label>
          <input type="text" class="form-control" name="last_name" id="last_name" value="<?= htmlspecialchars($newUser['last_name']) ?>">
        </div>

        <div class="mb-3">
          <label for="mail" class="form-label">Adresse mail</label>
          <input type="email" class="form-control" name="mail" id="mail" value="<?= htmlspecialchars($newUser['mail']) ?>">
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Mot de passe</label>
          <input type="password" class="form-control" name="password" id="password" value="<?= htmlspecialchars($newUser['password']) ?>">
        </div>

        <div class="form-check mb-4">
          <input class="form-check-input" type="checkbox" name="is_admin" id="is_admin" value="1" <?= $newUser['is_admin'] ? 'checked' : '' ?>>
          <label class="form-check-label" for="is_admin">
            Administrateur
          </label>
        </div>

        <div class="d-grid">
          <button type="submit" name="submit" class="btn btn-success">Valider</button>
        </div>
      </form>
    </div>
  </div>
</div>
